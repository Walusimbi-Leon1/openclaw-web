name: Host OpenClaw Web

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'How long to keep OpenClaw running (seconds, default 7200 = 2 hours)'
        required: false
        default: '7200'
      tunnel_type:
        description: 'Tunnel: "trycloudflare" (no account, random URL) or "cloudflared_named" (needs CF_ACCOUNT_ID + TUNNEL_TOKEN)'
        required: false
        default: 'trycloudflare'

concurrency:
  group: openclaw-web-session
  cancel-in-progress: false

jobs:
  openclaw-web:
    name: OpenClaw behind trycloudflare tunnel
    runs-on: ubuntu-22.04
    timeout-minutes: 150  # hard cap (2h30m)

    steps:
      # ── 1. Install system dependencies ──
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl git build-essential python3 unzip jq

      # ── 2. Install nvm + Node 26 ──
      - name: Setup Node 26
        run: |
          curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
          export NVM_DIR="$HOME/.nvm"
          . "$NVM_DIR/nvm.sh"
          nvm install 26
          nvm use 26
          nvm alias default 26
          node --version
          npx --version

      # ── 3. Install openclaw (GLOBALLY — required so the binary is on PATH) ──
      - name: Install openclaw
        run: |
          export NVM_DIR="$HOME/.nvm"
          . "$NVM_DIR/nvm.sh"
          nvm use 26 --silent
          npm install -g npm@latest
          # BUG FIX: must be -g or the openclaw binary is never on PATH and the
          # gateway start step fails with "command not found".
          npm install -g openclaw@latest
          # Make the global node bin dir available to every later step.
          NODE_BIN="$(dirname "$(command -v node)")"
          echo "$NODE_BIN" >> "$GITHUB_PATH"
          echo "node: $(command -v node) -> $(node --version)"
          echo "openclaw: $(command -v openclaw)"
          openclaw --version

      # ── 4. Write identity/workspace files (from Codespace-3 repo) ──
      - name: Fetch identity files from Codespace-3
        env:
          GH_PAT: '${{ secrets.GH_PAT_Codespace3 }}'
          GH_TOKEN_FALLBACK: '${{ secrets.GITHUB_TOKEN }}'
        run: |
          mkdir -p "$HOME/openclaw-workspace"
          BASE="https://raw.githubusercontent.com/Walusimbi-Leon1/Codespace-3/main"
          # Codespace-3 is a PRIVATE repo, so we need a token for raw access.
          # Prefer the dedicated PAT; fall back to the built-in GITHUB_TOKEN
          # (only works if Codespace-3 has granted this repo read access).
          AUTH_ARGS=()
          if [ -n "$GH_PAT" ]; then
            AUTH_ARGS=(-H "Authorization: Bearer $GH_PAT")
          elif [ -n "$GH_TOKEN_FALLBACK" ]; then
            AUTH_ARGS=(-H "Authorization: Bearer $GH_TOKEN_FALLBACK")
          fi
          # Core identity files required to boot as LA5.
          for f in SOUL.md IDENTITY.md USER.md MEMORY.md AGENTS.md HEARTBEAT.md TOOLS.md; do
            if ! curl -sfL "${AUTH_ARGS[@]}" "$BASE/$f" -o "$HOME/openclaw-workspace/$f"; then
              if [ "$f" = "MEMORY.md" ] || [ "$f" = "AGENTS.md" ] || [ "$f" = "HEARTBEAT.md" ] || [ "$f" = "TOOLS.md" ]; then
                echo "::warning::failed to fetch optional $f (continuing without it)"
              else
                echo "::error::failed to fetch required $f — check GH_PAT_Codespace3 secret"
                exit 1
              fi
            fi
          done
          echo "Identity files written:"
          ls -la "$HOME/openclaw-workspace/"

      # ── 5. Generate openclaw.json config ──
      - name: Configure OpenClaw
        id: config
        env:
          OPENCODE_API_KEY: '${{ secrets.OPENCODE_API_KEY }}'
          OPENCLAW_TOKEN_IN: '${{ secrets.OPENCLAW_GATEWAY_TOKEN }}'
          SESSION_KEY: '${{ secrets.SESSION_KEY }}'
        run: |
          WORKSPACE_DIR="$HOME/openclaw-workspace"
          mkdir -p "$HOME/.openclaw"
          # BUG FIX: if the token secret is empty, generate one so the emitted
          # token is always valid (otherwise the user could never log in).
          OPENCLAW_TOKEN="${OPENCLAW_TOKEN_IN:-$(openssl rand -hex 24)}"
          echo "OPENCLAW_TOKEN=$OPENCLAW_TOKEN" >> "$GITHUB_ENV"
          echo "token=$OPENCLAW_TOKEN" >> "$GITHUB_OUTPUT"
          # Optional session key (used as agent:main:<SESSION_KEY> in the UI URL).
          SKEY="${SESSION_KEY:-main}"
          cat > "$HOME/.openclaw/openclaw.json" << JSONEOF
          {
            "gateway": {
              "mode": "local",
              "port": 18789,
              "bind": "loopback",
              "auth": {
                "mode": "token",
                "token": "$OPENCLAW_TOKEN",
                "allowTailscale": false
              },
              "controlUi": {
                "enabled": true,
                "basePath": "/",
                "allowedOrigins": [],
                "dangerouslyAllowHostHeaderOriginFallback": true,
                "allowInsecureAuth": true,
                "dangerouslyDisableDeviceAuth": true
              },
              "tailscale": { "mode": "off", "resetOnExit": false }
            },
            "agents": {
              "defaults": {
                "workspace": "$WORKSPACE_DIR",
                "model": { "primary": "oc-hy3-free/hy3-free" },
                "timeoutSeconds": 86400
              },
              "main": { "workspace": "$WORKSPACE_DIR" }
            },
            "models": {
              "mode": "merge",
              "providers": {
                "oc-hy3-free": {
                  "baseUrl": "https://opencode.ai/zen/v1",
                  "api": "openai-completions",
                  "apiKey": "${OPENCODE_API_KEY}",
                  "timeoutSeconds": 86400,
                  "models": [{
                    "id": "hy3-free", "name": "oc/hy3-free (Primary)",
                    "contextWindow": 128000, "maxTokens": 4096,
                    "input": ["text"],
                    "cost": {"input": 0, "output": 0, "cacheRead": 0, "cacheWrite": 0},
                    "reasoning": false
                  }]
                },
                "oc-nemotron-3.5-lightning-free": {
                  "baseUrl": "https://opencode.ai/zen/v1",
                  "api": "openai-completions",
                  "apiKey": "${OPENCODE_API_KEY}",
                  "timeoutSeconds": 86400,
                  "models": [{
                    "id": "nemotron-3.5-lightning-free", "name": "oc/nemotron-3.5-lightning-free (Fast)",
                    "contextWindow": 128000, "maxTokens": 4096,
                    "input": ["text"],
                    "cost": {"input": 0, "output": 0, "cacheRead": 0, "cacheWrite": 0},
                    "reasoning": false
                  }]
                },
                "oc-laguna-s-2.1-free": {
                  "baseUrl": "https://opencode.ai/zen/v1",
                  "api": "openai-completions",
                  "apiKey": "${OPENCODE_API_KEY}",
                  "timeoutSeconds": 86400,
                  "models": [{
                    "id": "laguna-s-2.1-free", "name": "oc/laguna-s-2.1-free (Fallback)",
                    "contextWindow": 128000, "maxTokens": 4096,
                    "input": ["text"],
                    "cost": {"input": 0, "output": 0, "cacheRead": 0, "cacheWrite": 0},
                    "reasoning": false
                  }]
                },
                "custom-opencode-ai": {
                  "baseUrl": "https://opencode.ai/zen/v1",
                  "api": "openai-completions",
                  "apiKey": "${OPENCODE_API_KEY}",
                  "timeoutSeconds": 86400,
                  "models": [{
                    "id": "big-pickle", "name": "big-pickle (Last Resort)",
                    "contextWindow": 200000, "maxTokens": 16384,
                    "input": ["text"],
                    "cost": {"input": 0, "output": 0, "cacheRead": 0, "cacheWrite": 0},
                    "reasoning": false
                  }]
                }
              }
            },
            "session": { "dmScope": "per-channel-peer" },
            "tools": { "profile": "coding" }
          }
          JSONEOF
          python3 -c "import json; json.load(open('$HOME/.openclaw/openclaw.json')); print('Config OK')"
          echo "SESSION_KEY_USED=$SKEY" >> "$GITHUB_OUTPUT"

      # ── 6. Start OpenClaw gateway ──
      - name: Start OpenClaw gateway
        env:
          OPENCLAW_TOKEN: '${{ steps.config.outputs.token }}'
        run: |
          export NVM_DIR="$HOME/.nvm"
          . "$NVM_DIR/nvm.sh"
          nvm use 26 --silent
          cd "$HOME/.openclaw"
          # setsid + disown so the gateway survives the step's shell exit.
          setsid bash -c "openclaw gateway run --port 18789 --bind loopback > /tmp/openclaw-gateway.log 2>&1" &
          disown
          echo "OpenClaw launched."
          # Wait for the gateway to be up. /health may be auth-gated, so we accept
          # any non-connection-failure code (200/401/403). We also accept a
          # listening/ready log line as a success signal.
          HEALTHY=0
          for i in $(seq 1 60); do
            if grep -qiE "listening|ready|http server|gateway (is )?running|started" /tmp/openclaw-gateway.log 2>/dev/null; then
              HEALTHY=1; break
            fi
            CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 2 \
              -H "Authorization: Bearer $OPENCLAW_TOKEN" "http://127.0.0.1:18789/health" 2>/dev/null || echo "000")
            if [ "$CODE" != "000" ]; then
              HEALTHY=1; break
            fi
            sleep 1
          done
          if [ "$HEALTHY" != "1" ]; then
            echo "❌ Gateway did not become healthy"
            tail -40 /tmp/openclaw-gateway.log 2>/dev/null
            exit 1
          fi
          echo "✅ Gateway healthy on http://127.0.0.1:18789"

      # ── 7. Install cloudflared ──
      - name: Install cloudflared
        run: |
          sudo mkdir -p /opt/cloudflared
          sudo curl -sL -o /usr/local/bin/cloudflared \
            https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          sudo chmod +x /usr/local/bin/cloudflared
          cloudflared --version

      # ── 8. Start cloudflared tunnel → trycloudflare ──
      - name: Start cloudflared tunnel
        id: tunnel
        run: |
          # trycloudflare quick tunnel: dials OUT, no CF account, random subdomain.
          setsid bash -c "cloudflared tunnel --url http://127.0.0.1:18789 --no-autoupdate > /tmp/cloudflared.log 2>&1" &
          disown
          echo "Cloudflared launched; waiting for trycloudflare URL..."
          TUNNEL_URL=""
          for i in $(seq 1 40); do
            TUNNEL_URL=$(grep -oE 'https://[a-z0-9-]+\.trycloudflare\.com' /tmp/cloudflared.log 2>/dev/null | head -1 || true)
            if [ -n "$TUNNEL_URL" ]; then break; fi
            sleep 1
          done
          if [ -z "$TUNNEL_URL" ]; then
            echo "❌ Failed to acquire trycloudflare tunnel URL"
            echo "=== cloudflared.log (tail) ==="
            tail -30 /tmp/cloudflared.log 2>/dev/null
            exit 1
          fi
          echo "TUNNEL_URL=$TUNNEL_URL" >> "$GITHUB_OUTPUT"

      # ── 9. Verify the tunnel actually reaches the gateway ──
      - name: Verify tunnel reachability
        env:
          OPENCLAW_TOKEN: '${{ steps.config.outputs.token }}'
        run: |
          TUNNEL_URL="${{ steps.tunnel.outputs.TUNNEL_URL }}"
          echo "TUNNEL_URL=$TUNNEL_URL"
          # BUG FIX: the control UI may be auth-gated. Any of 200/401/403 means the
          # tunnel successfully reaches the gateway, so we accept those.
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 \
            -H "Authorization: Bearer $OPENCLAW_TOKEN" "$TUNNEL_URL/" || echo "000")
          echo "CONTROL_UI_HTTP_STATUS=$HTTP_CODE"
          if [ "$HTTP_CODE" = "000" ]; then
            echo "❌ Tunnel could not reach the gateway (HTTP 000 / connection failed)"
            echo "=== cloudflared.log ==="
            tail -15 /tmp/cloudflared.log 2>/dev/null
            exit 1
          fi
          echo "✅ Tunnel reaches the OpenClaw gateway (HTTP $HTTP_CODE)"

      - name: Emit connection details
        env:
          OPENCLAW_TOKEN: '${{ steps.config.outputs.token }}'
          TUNNEL_URL: ${{ steps.tunnel.outputs.TUNNEL_URL }}
          SESSION_KEY: ${{ steps.config.outputs.SESSION_KEY_USED }}
        run: |
          echo "================================"
          echo " OpenClaw Web UI is ready!"
          echo "================================"
          echo "FINAL_URL=$TUNNEL_URL"
          echo "GATEWAY_TOKEN=$OPENCLAW_TOKEN"
          echo "SESSION_KEY=$SESSION_KEY"
          echo "Open the URL in your browser — enter the token at the auth prompt."
          {
            echo "# OpenClaw Web UI is ready"
            echo ""
            echo "**URL:** $TUNNEL_URL"
            echo ""
            echo "**Gateway token:** \`$OPENCLAW_TOKEN\`"
            echo ""
            echo "Open the URL in your browser and enter the token when prompted."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Keep running for the specified duration
        run: |
          DURATION="${{ inputs.duration || '7200' }}"
          echo "Keeping OpenClaw alive for $DURATION seconds."
          sleep "$DURATION"
          echo "Duration complete."

      - name: Final logs
        if: always()
        run: |
          echo "=== OpenClaw Gateway Log (tail) ==="
          tail -30 /tmp/openclaw-gateway.log 2>/dev/null || echo "(no gateway log)"
          echo ""
          echo "=== Cloudflared Log (tail) ==="
          tail -15 /tmp/cloudflared.log 2>/dev/null || echo "(no cloudflared log)"
